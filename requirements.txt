# Lung-R1: Graph-CoT QA Generation Dependencies
# Install: pip install -r requirements.txt
#
# For GRPO training dependencies, see grpo/requirements.txt
# For SFT training, install LLaMA-Factory separately:
#   git clone https://github.com/hiyouga/LLaMA-Factory.git
#   cd LLaMA-Factory && pip install -e ".[torch,metrics,deepspeed]"

networkx>=3.0
aiohttp>=3.8.0
pandas>=1.3.0
numpy>=1.21.0
openpyxl>=3.0.0
requests>=2.28.0
tqdm>=4.64.0
